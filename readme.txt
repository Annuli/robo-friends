# robo-friends
This program uses the React.js framework to create a robot directory from a
pre-existing array of objects and a site (robohash.org) to create a robot avatar
based on the object.name property. The user can use a searchbox to locate an avatar
by name. 
